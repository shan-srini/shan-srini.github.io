# Shan's Personal Website

This is a personal website built using React and Vite.

## Getting Started

To get started, run the following commands:

```bash
npm install
npm run dev --port 3000 --host 0.0.0.0
```

## Deploying

Deploy using Cloudflare Pages
Check wrangler.jsonc for more details
Deploy will be handled automatically on push to main branch